language: python
python: 2.7
        3.6
sudo: false
env:
    - TOX_ENV=py27
    - TOX_ENV=py36
    - TOX_ENV=flake8
install:
    - pip install coverage python-coveralls tox
script:
    - tox -e $TOX_ENV
after_success:
    - coveralls
notifications:
    irc: "irc.freenode.org#rpappalax"
